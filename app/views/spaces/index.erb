<h1>Spaces</h1>

<ul id='list_spaces'>

  <% @spaces.each do |space| %>
      <li>
        <%= space.space_name %>
        <%= space.description %>
        <%= space.price %>
        <%= space.user.name %>
     </li>
  <% end %>

</ul>
